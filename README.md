# nvim-config
My personal NeoVim configuration using Lua. I am using Packer as a package manager.

# NOTES
- NerdFont
- tree-sitter (RUST)

For Windows you will need to configure alot of things using scoop and winget.
- diffutils for undo extension
- setting gcc as default compiler in terminal for tree-sitter
- setting git path in terminal
